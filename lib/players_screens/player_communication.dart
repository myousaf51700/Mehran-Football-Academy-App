import 'package:flutter/material.dart';

class PlayerCommunication extends StatelessWidget {
  const PlayerCommunication({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      mainAxisAlignment: MainAxisAlignment.center,
      children: [
        const Center(child: Text('Player Communication screen')),
        // Add any additional content or widgets specific to this screen here
      ],
    );
  }
}